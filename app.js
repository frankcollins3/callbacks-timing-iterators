function sayHello (name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
})

function sayHello (name) {
    let msg = 'Hello, ' + name + '. How are you?;
    return msg;'
}

function sayHello (name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

function sayHello (name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}

function sayHello (name) {
    let msg = 'Hello, ' + name + '. How are you?';
    return msg;
}